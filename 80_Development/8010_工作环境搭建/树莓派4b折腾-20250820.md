本来的想法是安装OpenMediaVault做个NAS试试，后来发现索性把它格式化（原来是ubuntu server 20.04系统），安装raspberry pi os lite 64bit，再加上一些docker之类的工具，当成一个随身的小工具算了。

## 系统安装
[[../../00_日志和总结/天/2025-08-18]]
本质上是Debian 12, 所以可以换源，用的ali源。

## 工具和服务
### smb
[[../../00_日志和总结/天/2025-08-20]]

### v2raya
科学上网

### docker工具类
portainer进行管理,注意各个port设置
![[imgs/Pasted image 20250821092721.png]]
```bash
# navidrome
# need to create /etc/navidrome/music && /etc/navidrome/data in advance
# Login username & pwd: rp4b
docker run -d --name navidrome --restart=unless-stopped -v /etc/navidrome/music:/music -v /etc/navidrome/data:/data -p 4533:4533 -e ND_LOGLEVEL=info deluan/navidrome:latest

# searnxg
docker pull docker.io/searxng/searxng:latest
# Create directories for configuration and persistent data
mkdir -p ./searxng/config/ ./searxng/data/
cd ./searxng/
# Run the container
docker run --name searxng -d \
    -p 8888:8080 \
    -v "./config/:/etc/searxng/" \
    -v "./data/:/var/cache/searxng/" \
    docker.io/searxng/searxng:latest

# Sun-panel
#Username: **[admin@sun.cc](mailto:admin@sun.cc)**
#Password: **12345678**
# changed to: admin, 12345678
docker run -d --restart=always -p 3002:3002 \
-v ~/docker_data/sun-panel/conf:/app/conf \
-v /var/run/docker.sock:/var/run/docker.sock \
--name sun-panel \
hslr/sun-panel:latest

# hivision_idphotos
docker pull linzeyi/hivision_idphotos
docker run -d -p 7860:7860 linzeyi/hivision_idphotos

# gitea
version: "3"

networks:
  gitea:
    external: false

services:
  server:
    image: gitea/gitea:1.21.1
    container_name: gitea
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=postgres
      - GITEA__database__HOST=db:5432
      - GITEA__database__NAME=gitea
      - GITEA__database__USER=gitea
      - GITEA__database__PASSWD=gitea
    restart: always
    networks:
      - gitea
    volumes:
      - ./gitea:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "18300:3000"
      - "222:22"
    depends_on:
      - db

  db:
    image: postgres:14
    restart: always
    environment:
      - POSTGRES_USER=gitea
      - POSTGRES_PASSWORD=gitea
      - POSTGRES_DB=gitea
    networks:
      - gitea
    volumes:
      - ./postgres:/var/lib/postgresql/data

```
除了这些，还有
1. docker-autocompose:  https://github.com/Red5d/docker-autocompose


