# install postgresql
1. 安装postgresql还是用的原生安装方式，直接装到了ubuntu里，版本16.2
2. 结果timescaledb就装不上了
用的 https://docs.timescale.com/self-hosted/latest/install/installation-source/ 
build from source方式，但是报错
![[imgs/Pasted image 20231204133021.png]]
盲猜还是不支持16.1版本的postgresql，等等把再

# arduino-ide
安装的2.0的版本，用的appimage
https://docs.arduino.cc/software/ide-v2/tutorials/getting-started/ide-v2-downloading-and-installing
官网安装步骤里最后一个，
> To enable the Arduino IDE to access the serial port and upload code to your board, the following rule can be added to 

```
/etc/udev/rules.d/99-arduino.rules
```
这个没用。

技巧：用命令行启动appimage，这样可以看到当前的实时状态。
报错1：一直卡在启动界面，后来发现是少一个文件，手动touch创建一下即可。
```bash
touch ~/.arduino15/library_index.json
```
然后重启ide即可。

报错2：我连接了

