#trace #w3c #log #span #observability

# trace-context w3c定义
里面包含了trace id啊，各个字段的含义。在定义observability的各种字段的时候，这个就成了必须项目。
https://www.w3.org/TR/trace-context/#:~:text=This%20specification%20defines%20standard%20HTTP%20headers%20and%20a,context%20information%20is%20sent%20and%20modified%20between%20services.

# 升级go版本（Ubuntu）
https://go.dev/doc/install
注意把里面的第一条命令加上两个`sudo`
```
sudo rm -rf /usr/local/go && sudo tar -C /usr/local -xzf go1.24.4.linux-amd64.tar.gz
```



# 安装opentelemetry collector
https://opentelemetry.io/docs/collector/quick-start/
注意：
1. 注意Prerequisites里的GOBIN路径设置
2. 
