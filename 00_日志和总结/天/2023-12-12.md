# .gitignore不起作用
## 问题描述
在项目里添加了.gitignore，但是不起作用。

## 解决办法
清除本地的git缓存，再重新建立索引。
```bash
# 0. 进入项目路径
# 1. 清除本地当前的Git缓存
git rm -r --cached .

# 2. 应用.gitignore等本地配置文件重新建立Git索引
git add .

# 3. （可选）提交当前Git版本并备注说明
git commit -m 'update .gitignore'

```

# VScode里Python导入module的问题
## 问题描述
在vscode里python导入其它路径或文件，用相对路径出现问题，写死绝对路径就没问题。
![[imgs/Pasted image 20231212131437.png]]
## 解决方案
默认的 python 配置并没有配置 cwd ，这个变量默认设置的是 `workspaceFolder`，所以这时候就需要我们将它手动改成 `fileDirname`。
在vscode的debug里创建launch.json，
![[imgs/Pasted image 20231212131839.png]]
https://zhuanlan.zhihu.com/p/360862550

# openai api key的代理
python里import openai后写入key再加一行openai.api_base = "https://api.qytech.top/v1"也不用爬梯子了。

# 科大讯飞大模型申请
打开[星火认知大模型](https://xinghuo.xfyun.cn/sparkapi)。。。
我只注册了，我没申请免费的，因为有效期一年，我怕申请了以后再折腾就没了。

# 各个大模型的api key
```txt
OPENAI_API_KEY = "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"

wenxin_api_key = "1BDStDsn0GUR6jM3vGqGVr3L"

wenxin_secret_key = "QkR83xhEr8qWX94uC2pxIUCuiaX18oBy"

spark_appid = "" #填写控制台中获取的 APPID 信息

spark_api_secret = "" #填写控制台中获取的 APISecret 信息

spark_api_key = "" #填写控制台中获取的 APIKey 信息

ZHIPUAI_API_KEY = "78f27ad693fd0b5f037d0cc62e6e3f80.g6LE7TW9E6MMCXmd"

HuggingFace_API_KEY="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"
```

