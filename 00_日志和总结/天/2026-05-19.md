# 继续部署在464C2上的

## filebrowser


## watchtower
1. [Watchtower：让 Docker 容器镜像保持自动更新 - 系统极客](https://www.sysgeek.cn/watchtower/)
2. [WATCHTOWER Docker 容器化部署指南 - 轩辕镜像技术博客 | Docker 镜像高效稳定拉取服务](https://xuanyuan.cloud/blog/watchtower-docker)
```bash
docker run -d \
  --name watchtower \
  --restart always \
  -e TZ=Asia/Shanghai \
  -e WATCHTOWER_CLEANUP=true \
  -e WATCHTOWER_LABEL_ENABLE=true \
  -e WATCHTOWER_HTTP_API_TOKEN=mytoken \
  -e WATCHTOWER_HTTP_API_METRICS=true \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -p 11001:8080 \
  nickfedor/watchtower
```


## filebrowser
[Installation - File Browser](https://filebrowser.org/installation.html)
```bash
docker run \
    -v /share/Private:/srv \
    -v /share/Container/filebrowser/database:/database \
    -v /share/Container/filebrowser/config:/config \
    -p 11002:80 \
    filebrowser/filebrowser
```
admin, Zz-TB8RQJColZ6Sg

## qbittorrent
没成功，报错“unauthorized”
```yaml
version: '3.8' 
services: 
	qbittorrent: 
		image: linuxserver/qbittorrent:latest 
		container_name: qbittorrent 
		restart: always 
		ports: 
			- 11003:8080 # WebUI访问端口 
			- 6881:6881 
			- 6881:6881/udp # 下载端口 
		volumes: 
			- /share/Container/qbittorrent/config:/config # 配置文件目录 
			- /share/Download:/downloads # 下载文件目录 
		environment: 
			- TZ=Asia/Shanghai 
			- PUID=1000
			- PGID=1000

```

## Alist

用myqnap软件源安装的
ysp，kfw6U6x2
警告	2026-05-19	13:27:45	System	127.0.0.1	---	---	---	---	[Alist] the temporary password from is kfw6U6x2

## Adguard Home
```bash
docker run -d \
  --name adguardhome \
  --restart unless-stopped \
  -v /share/Container/adguardhome/conf:/opt/adguardhome/conf \
  -v /share/Container/adguardhome/work:/opt/adguardhome/work \
  #-p 53:53/tcp -p 53:53/udp \  # DNS服务端口
  -p 3000:3000/tcp \            # 初始配置界面端口
  -p 80:80/tcp \                # HTTP服务端口（用于DNS-over-HTTPS等）
  -p 443:443/tcp -p 443:443/udp \  # HTTPS服务端口
  adguard/adguardhome:latest

```

ysp, yueyueniao3


## nginx-proxy-manager
```bash
docker run -d \
    --name=nginx-proxy-manager \
    -p 8181:8181 \  # Web管理界面端口
    -p 8080:8080 \  # HTTP反向代理端口（对外提供HTTP服务）
    -p 4443:4443 \  # HTTPS反向代理端口（对外提供HTTPS服务）
    -v /share/Container/nginx-proxy-manager:/config:rw \  # 持久化数据卷
    jlesage/nginx-proxy-manager

```
http://192.168.31.188:8181
用户名：ysp
email：408628446@qq.com
pwd: 408628446@QQ.com


## jackett

```yml

services:
  jackett:
    image: lscr.io/:latest
    container_name: jackett
    environment:
      - PUID=1000          # 用户ID（详见下文说明）
      - PGID=1000          # 组ID（详见下文说明）
      - TZ=Asia/Shanghai         # 时区，参考时区列表：      - AUTO_UPDATE=true   # 可选：允许容器内自动更新 Jackett
      - RUN_OPTS=          # 可选：传递额外启动参数
    volumes:
      - /share/Container/jackett/data:/config   # 本地 Jackett 配置文件路径
      - /share/Download/jackett-blackhole:/downloads   # 本地种子黑洞路径
    ports:
      - 9117:9117          # WebUI 端口映射
    restart: unless-stopped

```
