#todo #diary
# huggingface 下载提速
HuggingFace 模型下载提速
参考：huggingface 模型下载提速 - 知乎 https://zhuanlan.zhihu.com/p/691243939

在终端运行（或者将这个指令写在终端的配置文件的末尾，例如~/.bashrc或~/.zshrc）(前提是需要挂梯子)：
```bash
export HF_ENDPOINT=https://hf-mirror.com
```

现在的 HuggingFace 下载能够自动断点续传，所以不需要加参考文章中提到的其他杂七杂八的参数了。
