# CATL宁德时代 Catena-X Digital Twin
## 人物角色


## 资料和相关问题

|     | 资料                | URL                                                                                                                                                   | 问题                                                                           |
| --- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 0   | 环境部署              | [Deploying the components \| Eclipse Tractus-X](https://eclipse-tractusx.github.io/docs/tutorials/e2e/connect/deployComponents/)                      | 关于如何部署相关环境的，目前是单机环境k8s cluster                                               |
| 1   | Tractus-X DTR kit |                                                                                                                                                       | 参考下面1. 部署DTR部分                                                               |
| 2   | Industry Core kit | [Industry Core KIT \| Eclipse Tractus-X](https://tractus-x-community-days.github.io/eclipse-tractusx.github.io/docs-kits/category/industry-core-kit/) | Development view/digital twins 里有相关的概念解释，但里面的内容和实际的操作有一些出入，依旧需要further debug |
| 3   | BMW guide         |                                                                                                                                                       |                                                                              |






## 困难点和解决办法
### 1. 部署DTR
1. 开始时候使用的是Github上下载的helm chart的release，[eclipse-tractusx/sldt-digital-twin-registry](https://github.com/eclipse-tractusx/sldt-digital-twin-registry)，下载好之后解压，用helm install命令安装。安装前参考下一步。
2. 虽然有install.md([sldt-digital-twin-registry/INSTALL.md at main · eclipse-tractusx/sldt-digital-twin-registry](https://github.com/eclipse-tractusx/sldt-digital-twin-registry/blob/main/INSTALL.md)) 但这个不好用，实际用的是1. 里面下载的包里面的values.yml进行安装。
3. DTRservice有两个部分，一个是DTR服务本身，一个是postgresql数据库存储一些信息和认证。在2.里面的yml中，postgresql password有问题，应该是`""`表示空，但这里真的留空了。但在0.6.3的版本中，即使是修改成`""`，也无法正常启动，依旧显示postgresql的认证错误。
4. **解决方案**：联系了FT德国的同事`Batsiukov, Kiryl`，通过他联系到了外面的一个ext同事Dan，给了一份他们用来部署的values.yml。（需要和hetong要）
5. 部署好后，ingress里也要做相应修改，可以参考邮件。
```yml
# Hi Shao Peng,
#       测试环境DTR Values.yaml文件信息如下，需要改动的地方已用黄色高亮。(用#x开头)
#       官方的Char部署包地址[https://github.com/eclipse-tractusx/sldt-digital-twin-registry/releases](https://eur01.safelinks.protection.outlook.com/?url=https%3A%2F%2Fgithub.com%2Feclipse-tractusx%2Fsldt-digital-twin-registry%2Freleases&data=05%7C02%7CShaoPeng.Yang%40siemens.com%7C21601a2ef69642543c7e08dd22fd2a7e%7C38ae3bcd95794fd4addab42e1495d55a%7C1%7C0%7C638705192929460745%7CUnknown%7CTWFpbGZsb3d8eyJFbXB0eU1hcGkiOnRydWUsIlYiOiIwLjAuMDAwMCIsIlAiOiJXaW4zMiIsIkFOIjoiTWFpbCIsIldUIjoyfQ%3D%3D%7C0%7C%7C%7C&sdata=h0XBLk68Z42qerY%2Bn%2FIE%2FHPO%2FWBxP2ZPw3zVhQcVG0I%3D&reserved=0) 可做参考
nameOverride:
fullnameOverride:
# enables the default postgres database
#x
enablePostgres: false
# enables the default keycloak identity provider
# relies on a postgres instance
enableKeycloak: false
registry:
  image:
    registry: docker.io
    repository: tractusx/sldt-digital-twin-registry
    # This version property overwrites the default image version.
    # The default image version is the appVersion of the Chart.yaml.
    version: ""
  replicaCount: 1
  imagePullPolicy: IfNotPresent
  containerPort: 4243
#x
  host: YOUR_HOST
  ## If 'authentication' is set to false, no OAuth authentication is enforced
  authentication: false
  # Issuer url for the dtr (resource server),
  # make sure that the url points to an externally resolvable hostname.
  # If no value is committed, and the integrated Keycloak is enabled,
  # the K8s internal service name is used, which is a problem, when
  # validating the issuer claim in an access token
  idpIssuerUri: ""
  idpClientId: default-client
#x
  tenantId: YOUR_BPN
  # this field is necessary if cognito is used as identity provider instead of keycloak
  idpInternalClientId: default-client
  # this field configures the identity provider for the digital twin registry (keycloak or cognito)
  identityProvider: keycloak
  externalSubjectIdWildcardPrefix: PUBLIC_READABLE
  externalSubjectIdWildcardAllowedTypes: manufacturerPartId,digitalTwinType
  useGranularAccessControl: "false"
  granularAccessControlFetchSize: "500"
  service:
    port: 8080
    type: ClusterIP
  livenessProbe:
    failureThreshold: 3
    periodSeconds: 3
    initialDelaySeconds: 100
  readinessProbe:
    failureThreshold: 3
   periodSeconds: 3
    initialDelaySeconds: 100
  dataSource:
    driverClassName: org.postgresql.Driver
    sqlInitPlatform: pg
    ## The url, user, and password parameter will be ignored if 'enablePostgres' is set to true.
    ## In that case the postgresql auth parameters are used.
#x
    url: YOUR_JDBC_URL
#x
    user: YOUR_USER
#x
    password: YOUR_PASSWORD
  ingress:
    enabled: true
    tls: false
    urlPrefix: /semantics/registry
    className: nginx
    annotations:
      # Add annotations for the ingress, e.g.:
      # cert-manager.io/cluster-issuer: selfsigned-cluster-issuer
#x
      nginx.ingress.kubernetes.io/rewrite-target: /$2
      # nginx.ingress.kubernetes.io/use-regex: "true"
      # nginx.ingress.kubernetes.io/enable-cors: "true"
      # nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
      # nginx.ingress.kubernetes.io/x-forwarded-prefix: /semantics/registry
  resources:
    limits:
      cpu: 750m
      memory: 1024Mi
    requests:
      cpu: 250m
      memory: 1024Mi
  podSecurityContext:
    runAsUser: 100
  securityContext:
    runAsUser: 100
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
```

### 2. Asset定义好之后不可见
1. 原因：BMW手册里的步骤，定义的AccessPolicy是给特定的Business Part Number Group，CATL不在这个里面。
2. **解决方案**：把AccessPolicy修改为全部可见。

### 3. 获取不到DTR submodel的contract
1. 原因：contract definition中contractPolicyId指向不对。这个字段其实就是UsagePolicyId
	![[imgs/Pasted image 20241223104648.png]]
	![[imgs/Pasted image 20241223104722.png]]
2. 解决方案：copy对应的Usage Policy Id过来就好了