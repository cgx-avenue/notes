# 火山方舟
api-key: ark-88c0fb9e-8fb6-4d56-96fc-6314c0ebf5de-8c3a4
```bash
curl https://ark.cn-beijing.volces.com/api/v3/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ark-88c0fb9e-8fb6-4d56-96fc-6314c0ebf5de-8c3a4" \
  -d '{
    "model": "doubao-1-5-pro-32k-250115",
    "messages": [
      {"role": "system","content": "你是人工智能助手."},
      {"role": "user","content": "你好"}
    ]
  }'
```

# 硅基流动
手机号登陆
api key: sk-qvaldfggizqciclncdhcgpmqlvxjpnefqeavuosiqwqhbnzg
```env
OPENAI_API_KEY=你的硅基流动API_KEY OPENAI_BASE_URL=https://api.siliconflow.cn/v1 
OPENAI_MODEL=doubao-seed-2-0-pro
```
```bash
curl --request POST \
  --url https://api.siliconflow.cn/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "deepseek-ai/DeepSeek-V4-Flash",
    "messages": [
      {"role": "system", "content": "你是一个有用的助手"},
      {"role": "user", "content": "你好，请介绍一下你自己"}
    ]
  }'
```



rcxi27eq7hzj0pedwl.xuanyuan.run
docker pull rcxi27eq7hzj0pedwl.xuanyuan.run/birdxs/daily_stock_analysis
docker pull rcxi27eq7hzj0pedwl.xuanyuan.run/lissy93/dashy
# daily_stock_analysis
怎么都弄不好，真的不想弄了
```bash
# Web/API 模式
docker pull zhulinsen/daily_stock_analysis:latest
docker run -d \
  --name dsa-server \
  --env-file .env \
  -p 8000:8000 \
  -v "$(pwd)/data:/app/data" \
  -v "$(pwd)/logs:/app/logs" \
  -v "$(pwd)/reports:/app/reports" \
  -v "$(pwd)/.env:/app/.env" \
  rcxi27eq7hzj0pedwl.xuanyuan.run/birdxs/daily_stock_analysis:latest \
  python main.py --serve-only --host 0.0.0.0 --port 8000

# 定时任务模式
docker run -d \
  --name dsa-analyzer \
  --env-file .env \
  -v "$(pwd)/data:/app/data" \
  -v "$(pwd)/logs:/app/logs" \
  -v "$(pwd)/reports:/app/reports" \
  -v "$(pwd)/.env:/app/.env" \
  rcxi27eq7hzj0pedwl.xuanyuan.run/birdxs/daily_stock_analysis:latest
```
## Dashy
弄一个导航页面试一试docker的问题
```bash
docker run -d \
  --name dashy \
  -p 38080:80 \
  -v /path/to/your/config.yml:/app/public/conf.yml \
  --restart unless-stopped \
  rcxi27eq7hzj0pedwl.xuanyuan.run/lissy93/dashy

```
