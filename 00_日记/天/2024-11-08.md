# streamlit vscode debug调试

主要是vscode的`launch.json`文件。
## Steps
1. vscode里选择正确的python环境
2. debugger里新建`launch.json`，选择第一个，`python 文件`
3. 在新建的`launch.json`文件里copy如下内容

```json
{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python:Streamlit",
            "type": "debugpy",
            "request": "launch",
            "module": "streamlit",
            "args": [
                "run",
                "${file}",
                "--server.port",
                "2000"
            ]
        }
    ]
}
```

## args详解
1. `${file}` 直接代表当前文件，不需要每次更换文件名，只要其中包含streamlit。
2. `port` 设置为2000。
3. 其他配置按需添加



# pandas-AI prompt & result
都用的water_pump.csv
## 时序阈值图

```txt
帮我生成Sensor_01的时序图，并在y轴添加三组阈值标志线：1. LCL 两条线，分别是均值加1倍标准差和均值减1倍标准差；2. MCL两条线，分别是均值加2倍标准差和均值减2倍标准差；3. UCL两条线，分别是均值加3倍标准差和均值减3倍标准差
```

![[../../_assets/img/Pasted image 20241111103418.png]]

## 频域阈值图

```txt
请分成两步完成基于数据的频谱图：第一步请画出数据对应的频谱图，横坐标和纵坐标都以对数作为维度。第二步，检查频谱数据，如果有频率点对应的幅度值超过10,则在频谱图中用红色的点标识出。
```

![[../../_assets/img/Pasted image 20241111143110.png]]