# Obsidian中git不能同步
显示用户和权限不对，输入如下代码，注意路径需要按需替换：
```bash
git config --global --add safe.directory C:/dev/notes
```

# 在终端和jupyter中查看当前运行的是哪个kernel
在终端和jupyter中，输入
```python
import sys
sys.executable
```

![[imgs/Pasted image 20240619100152.png]]

# LangServe里允许用ip访问
LangServe里允许除本机外的访问，本质是FastAPI允许除本机外的访问，用如下命令
```python
# 4. App definition
app = FastAPI(
  title="LangChain Server",
  version="1.0",
  description="A simple API server using LangChain's Runnable interfaces",
)

# 5. Adding chain route

add_routes(
    app,
    chain,
    path="/chain",
)

if __name__ == "__main__":
    import uvicorn

    uvicorn.run(app, host="0.0.0.0", port=8004)
```
![[imgs/Pasted image 20240619105026.png]]

# jupyterlab 登陆时候查看token是多少
shell里执行
```shell
jupyter notebook list
```


